let oopsNull = null; // do not touch it
// {{ You cannot change the equality sign(==) to != }}
if(typeof oopsNull == "object") { // We will learn more about comparisions in other session
    console.log("This is secret"); // output this
}